# Datos generados por el pipeline: se rehacen corriendo los scripts de src/.
# No se versionan para no guardar el mismo CSV una y otra vez en el historial.
# Regla: se versiona lo que NO se puede regenerar (reference/), no lo que si.
data/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Editores / SO
.vscode/
.idea/
.DS_Store
Thumbs.db
